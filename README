SDL2 OpenGL ES Test Applications
================================

These two applications test touch input and OpenGL ES context creation
of SDL 2.0 when using in combination with the Wayland video driver.
